-- 코드를 작성해주세요
SELECT 
    ITEM_ID,
    ITEM_NAME,
    RARITY
FROM
    ITEM_INFO
WHERE
    ITEM_ID IN
    (SELECT PARENT_ITEM_ID
    FROM ITEM_TREE
    GROUP BY
    PARENT_ITEM_ID) IS NULL
ORDER BY
    ITEM_ID DESC
;